# Move pnpm's content-addressable store outside the workspace so it does not
# clutter the project tree. ${HOME} resolves at runtime (in the devcontainer
# this is /home/node).
store-dir=${HOME}/.pnpm-store
